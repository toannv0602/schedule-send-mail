<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.schedulesendmaildemo.mapper.ProductMapper" >
    <!--<cache />-->
    <resultMap id="BaseResultMap" type="com.example.schedulesendmaildemo.entity.Product" >
        <id column="id" property="id"  />
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="quantity" property="quantity" />
        <!--One-on-one-->
        <association property="category"  javaType="com.example.schedulesendmaildemo.entity.Category" >
            <id property="idCat" column="id_cat"/>
            <result property="codeCat" column="code_cat"/>
            <result property="nameCat" column="name_cat"/>
        </association>
    </resultMap>
    <sql id="Base_Column_List">
        tb_product.id,tb_product.code, tb_product.name, quantity, tb_category.id as cat_id, tb_category.code as cat_code, tb_category.name as cat_name
    </sql>
    <select id="findAll" resultMap="BaseResultMap">
        select
            tb_product.*, tb_category.*
        from tb_product, tb_category
        where tb_product.categoty_id = tb_category.id_cat
    </select>
    <select id="getById" resultMap="BaseResultMap">
        select tb_product.*, tb_category.id as cat_id, tb_category.code as cat_code, tb_category.name as cat_name
        from tb_product, tb_category
        where  tb_product.id = #{id} and tb_product.categoty_id = tb_category.id
    </select>
    
    
</mapper>